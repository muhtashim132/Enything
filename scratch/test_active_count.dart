import 'package:supabase_flutter/supabase_flutter.dart';
import 'package:flutter_test/flutter_test.dart';

void main() async {
  // We can't really run this without supabase initialized, but we can check if it compiles.
}
