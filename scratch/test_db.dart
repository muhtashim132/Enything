import 'package:supabase_flutter/supabase_flutter.dart';

void main() async {
  final client = SupabaseClient(
    'placeholder', 
    'placeholder'
  );
  // Wait, I can't just connect from a random dart script unless I have the URL and anon key.
  // Can I read it from the environment or config?
}
